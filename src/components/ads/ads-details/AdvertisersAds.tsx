import React from "react";
import Card from "@/components/shared/Card";
import kingdom from "@/public/images/kingdom.jpg";
import AdsCards from "../AdsCards";

const AdvertisersAds: React.FC = () => {
    return (
        <div className="flex flex-wrap justify-center items-center lg:gap-4 gap-10 mt-10">
          {/* <AdsCards open={open} items={userData.ads} setOpen={setOpen}  /> */}

          kjlkl
        </div>
    );
};

export default AdvertisersAds;
